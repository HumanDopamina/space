# Distinción entre proyecto y aplicación

El aprendiz identificó correctamente que el modelo `Producto` pertenece a `catalogo/models.py` y que la aplicación debe registrarse en `config/settings.py`, junto con `rest_framework`. Esta base permite avanzar hacia modelos y migraciones; las próximas lecciones deben explicar primero el concepto y luego pedir la recuperación.

**Evidence:** respondió al ejercicio de la lección y verificó que el servidor mostraba la página inicial de Django.
